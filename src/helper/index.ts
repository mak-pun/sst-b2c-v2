import $ from './jquery'
import * as format from  './format'

let dzsprx_init: any = (window as any)["dzsprx_init"];
let Custombox: any = (window as any)["Custombox"];
let Omise: any = (window as any)['Omise'] 

export {$, dzsprx_init, Custombox, Omise, format}